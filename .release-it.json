{
  "git": {
    "commitMessage": "chore(release): v${version}",
    "changelog": "npx auto-changelog --stdout --commit-limit false -u --template keepachangelog"
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "after:bump": "npx auto-changelog -p"
  }
}